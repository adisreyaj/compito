import { ModalActionsDirective } from './modal-actions.directive';

describe('ModalActionsDirective', () => {
  it('should create an instance', () => {
    const directive = new ModalActionsDirective();
    expect(directive).toBeTruthy();
  });
});
