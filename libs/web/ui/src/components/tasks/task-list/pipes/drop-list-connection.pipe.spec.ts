import { DropListConnectionPipe } from './drop-list-connection.pipe';

describe('DropListConnectionPipe', () => {
  it('create an instance', () => {
    const pipe = new DropListConnectionPipe();
    expect(pipe).toBeTruthy();
  });
});
